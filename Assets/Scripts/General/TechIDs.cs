using UnityEngine;

public enum TechIDs
{
    j1,      // 弱1段
    j2,      // 弱2段
    st,      // 横強
    ut,      // 上強
    dt,      // 下強
    DA,      // ダッシュアタック
    ss,      // 横スマ
    us,      // 上スマ
    ds,      // 下スマ
    na,      // 空N
    fa,      // 空前
    ba,      // 空後
    ua,      // 空上
    da,      // 空下
    nb_c,    // NB（タメ）
    nb_a,    // NB（攻撃）
    sb,      // 横B
    ub_g,    // 上B（地上）
    ub_a,    // 上B（空中）
    db_g,    // 下B（地上）
    db_a,    // 下B（空中）
    g,       // つかみ
    ga,      // つかみ攻撃
    fth,     // 前投げ
    bth,     // 後投げ
    uth,     // 上投げ
    dth,     // 下投げ
    fc,      // 前投げ（前派生）
    bc,      // 前投げ（後派生）
    uc,      // 前投げ（上派生）
    dc,      // 前投げ（下派生）
    s,       // シールド
    nd,      // その場回避
    sd,      // 横回避
    ad       // 空中回避
}